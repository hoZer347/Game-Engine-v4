#pragma once

#include <mutex>
#include <atomic>
#include <vector>
#include <functional>

namespace loom
{
	typedef unsigned int uint32;
	typedef std::vector<float>	Vtxs;
	typedef std::vector<uint32>	Inds;

	struct Texture;
	struct Shader;

	struct Mesh
	{
		
	};
};
